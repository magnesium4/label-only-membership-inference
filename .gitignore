# private planning / research log (stays local)
PLAN.md

# datasets (regenerated by torchvision on download=True)
data/
cifar-10-python.tar.gz
cifar-10-batches-py/

# model checkpoints — ignored by default, since Phase 4 (shadow models) will produce
# many of them. The Phase 1 target is exempted deliberately: publishing it is what makes
# the committed correctness matrices checkable, given that retraining can't reproduce it.
*.pt
*.pth
!target_model.pt

# python
__pycache__/
*.pyc
.venv/

# local tooling
.claude/

# macOS
.DS_Store
